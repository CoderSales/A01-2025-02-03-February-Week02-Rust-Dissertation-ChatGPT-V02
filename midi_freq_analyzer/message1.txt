
____________

### memory update: 

Please give lines like this:

```
                            let max_amp = raw_data.iter().cloned().fold(0.0_f64, |a, b| a.max(b.abs())).max_by(|a, b| a.partial_cmp(b).unwrap()).unwrap_or(1e-12);
```

not dot dot dot in parentheses as I Ctrl F for what you give me and it does not come up until I remove the dot dot dot.

_____________________

Now:

here is update in snippet.txt